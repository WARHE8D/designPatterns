# Momento Pattern

This pattern helps keep the previous state of an object without affecting the encapsulation.
Just like an undo operation.
Only con is that if the object is large it might consume large memory to store the state.